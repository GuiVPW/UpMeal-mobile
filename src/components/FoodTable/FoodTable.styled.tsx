import styled from 'styled-components'

import { DataTable } from 'react-native-paper'

export const StyledCell = styled(DataTable.Cell)`
	width: 120px;
	display: flex;
	justify-content: center;
`

export const StyledHeader = styled(DataTable.Title)`
	width: 120px;
	display: flex;
	justify-content: center;
`
